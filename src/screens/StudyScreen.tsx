import { ScreenHeader } from "@/components/ScreenHeader";
import { STUDY_AREAS, type StudyArea } from "@/domains/learning/learning";
import { useGameStore } from "@/stores/game-store";
export function StudyScreen() { const study = useGameStore((s) => s.study); const learning = useGameStore((s) => s.learning); return <section className="screen padded"><ScreenHeader eyebrow="CHOOSE YOUR TRAINING" title="오늘은 무엇을 익힐까요?"/><p className="intro">공부할 영역만 선택하세요. 전투는 성장한 능력치로 자동 진행됩니다.</p><div className="study-list">{STUDY_AREAS.map((area) => <StudyCard key={area.id} area={area} xp={learning[area.id]} onStudy={study}/>)}</div><p className="debug-note">프로토타입용 임시 학습 버튼입니다. +50 XP마다 해당 능력치가 즉시 성장합니다.</p></section>; }
function StudyCard({ area, xp, onStudy }: { area: typeof STUDY_AREAS[number]; xp: number; onStudy: (id: StudyArea, xp: number) => void }) { return <article className="study-card" style={{ "--area": area.color } as React.CSSProperties}><span className="study-glyph">{area.glyph}</span><div><small>{area.ja}</small><h2>{area.ko}</h2><p>{area.stat} 성장</p><i><u style={{ width: `${xp % 100}%` }}/></i><em>누적 {xp} XP</em></div><button onClick={() => onStudy(area.id, 50)}>+50 XP</button></article>; }
