import { ScreenHeader } from "@/components/ScreenHeader";
import { useGameStore, type GameSettings } from "@/stores/game-store";
const options: Array<[keyof GameSettings, string, string]> = [["music","배경음","탑의 배경 음악"],["sound","효과음","공격과 승리 효과음"],["haptics","진동","Android 짧은 타격 진동"],["reducedMotion","연출 줄이기","움직임과 화면 흔들림 최소화"]];
export function SettingsScreen() { const s = useGameStore(); return <section className="screen padded"><ScreenHeader eyebrow="OPTIONS" title="설정"/><div className="settings-list">{options.map(([key,title,desc]) => <button key={key} onClick={() => s.toggleSetting(key)}><span><b>{title}</b><small>{desc}</small></span><i className={s.settings[key] ? "on" : ""}><u/></i></button>)}</div><button className="menu-button" onClick={() => s.navigate("debug")}><span>🛠</span><div><b>개발용 디버그</b><small>층 이동 · 저장 초기화 · 규칙 검증</small></div><em>›</em></button><div className="about"><span>語</span><div><b>Kotoba Tower</b><small>Prototype 0.2.0 · 오프라인 저장</small></div></div></section>; }
